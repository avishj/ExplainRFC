name: Lighthouse Audit (Scheduled)

on:
  schedule:
    - cron: '0 1 * * 0'
  workflow_dispatch:

jobs:
  lighthouse:
    uses: ./.github/workflows/_lighthouse.yml
    permissions:
      contents: write
      issues: write
